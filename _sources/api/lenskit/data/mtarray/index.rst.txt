lenskit.data.mtarray
====================

.. py:module:: lenskit.data.mtarray

.. autoapi-nested-parse::

   Multi-format array layout.



Attributes
----------

.. autoapisummary::

   lenskit.data.mtarray.NPT
   lenskit.data.mtarray.MTFloatArray
   lenskit.data.mtarray.MTIntArray
   lenskit.data.mtarray.MTGenericArray


Classes
-------

.. autoapisummary::

   lenskit.data.mtarray.MTArray



Module Contents
---------------

.. py:data:: NPT

.. py:class:: MTArray(array)
   

   Bases: :py:obj:`Generic`\ [\ :py:obj:`NPT`\ ]


   Multi-typed array class, allowing arrays to be easily converted between
   NumPy, PyTorch, and other supported backends, caching the conversion result.

   We use this class instead of one canonical format so that data can be
   converted lazily, and can be left in-place when passing from one component
   to another that use the same computational engine.

   .. note::

       This class is only intended for read-only arrays.  It is **not defined**
       whether the different arrays share storage, and modifying one format may
       or may not modify another.  For example, PyTorch and NumPy usually share
       storage when both are on CPU, but a GPU tensor and CPU ndarray do not.

   :Stability: Internal


   .. py:method:: wrap(array: numpy.typing.NDArray[NPT] | torch.Tensor | pyarrow.Array | pyarrow.Tensor | Sequence | numpy.typing.ArrayLike) -> Self
                  wrap(array: numpy.typing.NDArray[NPT] | torch.Tensor | pyarrow.Array | pyarrow.Tensor | Sequence | numpy.typing.ArrayLike | None) -> Self | None
      :classmethod:



   .. py:method:: scatter(dst, idx, src, *, mask = None)
      :classmethod:


      “Scatter” the values of a source array into positions of a destination
      array, or into a new array.



   .. py:property:: shape
      :type: tuple[int, Ellipsis]



   .. py:method:: numpy()

      Get the array as a NumPy array.



   .. py:method:: torch(*, device: str | None = None, convert: Literal[True] = True) -> torch.Tensor
                  torch(*, device: str | None = None, convert: bool = True) -> torch.Tensor | None

      Get the array as a PyTorch tensor.

      :param device: The device on which the Torch tensor should reside.
      :param convert: If ``False``,



   .. py:method:: arrow()

      Get the array as an Arrow :class:`~pyarrow.Array`.



   .. py:method:: to(format: Literal['numpy']) -> numpy.typing.NDArray[NPT]
                  to(format: Literal['torch'], *, device: str | None) -> torch.Tensor
                  to(format: Literal['arrow']) -> pyarrow.Array | pyarrow.Tensor
                  to(format: LiteralString, *, device: str | None = None) -> numpy.typing.ArrayLike

      Obtain the array in the specified format (dynamic version).



   .. py:method:: __array__(dtype=None, copy=None)


.. py:data:: MTFloatArray

.. py:data:: MTIntArray

.. py:data:: MTGenericArray


Exported Aliases
----------------

.. py:function:: lenskit.data.mtarray.safe_tensor

    Re-exported alias for :py:func:`lenskit.torch.safe_tensor`.

