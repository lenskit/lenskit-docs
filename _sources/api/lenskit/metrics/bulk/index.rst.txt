lenskit.metrics.bulk
====================

.. py:module:: lenskit.metrics.bulk


Attributes
----------

.. autoapisummary::

   lenskit.metrics.bulk.K1
   lenskit.metrics.bulk.K2


Classes
-------

.. autoapisummary::

   lenskit.metrics.bulk.RunAnalysisResult
   lenskit.metrics.bulk.RunAnalysis



Module Contents
---------------

.. py:data:: K1

.. py:data:: K2

.. py:class:: RunAnalysisResult(lmvs, gmvs, defaults)
   

   Results of a bulk metric computation.

   :Stability: Caller


   .. py:method:: global_metrics()

      Get the global metric scores.  This is only the results of
      global metrics; it does not include aggregates of per-list metrics.  For
      aggregates of per-list metrics, call :meth:`list_summary`.



   .. py:method:: list_metrics(fill_missing=True)

      Get the per-list scores of the results.  This is a data frame with one
      row per list (with the list key on the index), and one metric per
      column.

      :param fill_missing: If ``True`` (the default), fills in missing values with each
                           metric's default value when available.  Pass ``False`` if you
                           want to do analyses that need to treat missing values
                           differently.



   .. py:method:: list_summary(*keys)

      Summary statistics for the per-list metrics.  Each metric is on its own
      row, with columns reporting the following:

      ``mean``:
          The mean metric value.
      ``median``:
          The median metric value.
      ``std``:
          The (sample) standard deviation of the metric.

      Additional columns are added based on other options.  Missing metric
      values are filled with their defaults before computing statistics.

      :param keys: Identifiers for different conditions that should be reported
                   separately (grouping keys for the final result).



   .. py:method:: merge_from(other)

      Merge another set of analysis results into this one.



.. py:class:: RunAnalysis(*metrics)
   

   Compute metrics over a collection of item lists composing a run.

   This class now uses :class:`MetricAccumulator` internally to separate
   accumulation from looping, while maintaining the same external interface.

   :param metrics: A list of metrics; you can also add them with :meth:`add_metric`,
                   which provides more flexibility.

   :Stability: Caller


   .. py:method:: add_metric(metric, label = None, default = None)

      Add a metric to this metric set.

      :param metric: The metric to add to the set.
      :param label: The label to use for the metric's results.  If unset, obtains
                    from the metric.
      :param default: The default value to use in aggregates when a user does not have
                      recommendations. If unset, obtains from the metric's ``default``
                      attribute (if specified).



   .. py:property:: metrics
      :type: list


      The list of metrics to compute.

      .. deprecated:: 2025.4
          Access metrics through the accumulator interface instead.


   .. py:method:: compute(outputs, test)

      Deprecated alias for :meth:`measure`.

      .. deprecated:: 2025.1.1
          Use :meth:`measure` instead.



   .. py:method:: measure(outputs, test)

      Measure a set of outputs against a set of test data.




Exported Aliases
----------------

.. py:class:: lenskit.metrics.bulk.ItemList

    Re-exported alias for :py:class:`lenskit.data.ItemList`.

.. py:class:: lenskit.metrics.bulk.ItemListCollection

    Re-exported alias for :py:class:`lenskit.data.ItemListCollection`.

.. py:exception:: lenskit.metrics.bulk.DataWarning

    Re-exported alias for :py:exc:`lenskit.diagnostics.DataWarning`.

.. py:function:: lenskit.metrics.bulk.item_progress

    Re-exported alias for :py:func:`lenskit.logging.item_progress`.

.. py:class:: lenskit.metrics.bulk.Metric

    Re-exported alias for :py:class:`lenskit.metrics._base.Metric`.

.. py:class:: lenskit.metrics.bulk.MetricFunction

    Re-exported alias for :py:class:`lenskit.metrics._base.MetricFunction`.

